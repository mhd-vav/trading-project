// src/app/app/settings/page.tsx
import SettingsCenter from "@/components/settings/SettingsCenter";

export const metadata = { title: "تنظیمات فضای کاری" };

export default function SettingsPage() {
  return <SettingsCenter />;
}
